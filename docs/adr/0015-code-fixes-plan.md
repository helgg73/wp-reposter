# ADR 0015: План правок кода под ADR

- **Status:** Accepted
- **Date:** 2026-09-15
- **Related:** все ADR 0002–0013

## Context

После ревизии ADR выявлены расхождения между решениями и кодом. Этот документ — план правок. Код не трогаем без явного согласования.

## Already fixed (внесено в код)

| # | Правка | ADR | Что сделано |
|---|--------|-----|-------------|
| 1 | Устранить дублирование Pydantic-моделей | 0012 | `src/config.py` теперь только загружает YAML; все модели — в `src/models.py` |
| 2 | Синхронизировать `target-version` и `requires-python` | 0003 | `requires-python = ">=3.12"`, `target-version = "py312"` |
| 3 | Исправить докстринг `update_cutoff_date` | 0007 | «самый новый пост, который мы взяли в работу» |
| 4 | `disable_link_preview = True` в `models.py` | 0011 | Дефолт изменён с `False` на `True` |

## Deferred (отложено)

| # | Правка | ADR | Когда |
|---|--------|-----|-------|
| 5 | Добавить `uv.lock` в дамп TOC | 0002 | При следующем обновлении TOC |
| 6 | Добавить `.env.example` в дамп TOC | 0006 | При следующем обновлении TOC |
| 7 | Перевод `WordPressParser` на `AsyncClient` | 0013 | Этап 2 |
| 8 | Уточнить `_should_exclude()` (порядок `wp:term`) | 0004 | Этап 2 |
| 9 | Батчинг `_save()` в `StateManager` | 0009 | Этап 2 |
| 10 | Проверка `published` на `null` в cutoff_date | 0007 | Этап 2 |

## Not to touch

- `0001-project-dump-for-llm-context.md` — исторический тут же разработан скрипт для дампа репозитория.
- Код — только после согласования плана.