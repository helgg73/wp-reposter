Продолжаем разработку проекта wp-reposter.

Контекст:
- Проект: wp-reposter — репостер свежих публикаций с WordPress-сайтов
  в мессенджер MAX (и в будущем — на другие площадки).
- Текущий этап: Этап 2 (фильтрация по рубрикам/тегам + unit-тесты).
- Язык общения: русский.
- Формат ответов: как в ADR-файлах (Status / Context / Decision / Tasks /
  Done criteria / Not to touch).
- Файлы с вложенными блоками кода оборачивай в ЧЕТЫРЕ бэктика снаружи
  (````markdown ... ````), чтобы вложенные тройные не ломали вывод.

Что сделано:
- Этап 1 завершён: WP REST API → MAX, cutoff_date, state.json.
- ADR 0002–0015 зафиксированы.
- ROADMAP, stage-файлы 0016–0019, BACKLOG — созданы.
- .env.example в дампе TOC — исправлено.
- uv.lock в дамп TOC не включается осознанно (ADR 0001).

Что нужно сейчас:
<сформулируй задачу: например, «выполни S2-01, S2-02, S2-03» или
«пересмотри ADR 0017 в свете новых требований»>

Прикладываю файлы:
- 0001-project-dump-for-llm-context.md (исторический дамп, не трогаем)
- 0002…0015 (ADR)
- ROADMAP.md
- 0016…0019 (stage-файлы)
- BACKLOG.md
- project_dump.txt (актуальный дамп кода)